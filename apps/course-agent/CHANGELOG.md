# 更新日志

本文件记录课程复习 Agent 的主要功能、修复、文档和验证变化。内容以 Git 提交为依据；尚未实现的规划会明确标记为待办，不计入已交付功能。

## 2026-08-20：首页多模型选择与视觉微调

- 首页 Logo 放大到 88 px，标语调整为“真理如瀚海 求索亦行舟”，并为移动端保留词组级换行。
- Featured 工作台优先使用 Hub 下发的安全模型目录；模型选择器改为原生下拉框，支持 Luna、Sol、Terra 的真实选择与服务端校验，不再只是输入框外观切换。
- 平台 Grant 请求携带可选 `requested_model_id`，瀚海行把所选模型传到 Hub Gateway；平台不可用时仍回退既有本地模型配置。
- 模型与思考强度选择器改为柔和胶囊控件，静态资源版本提升到 `20260820-3`，避免浏览器命中旧页面。
- 新增平台模型目录隔离、Grant 模型选择和本地回退测试，并更新首页 DOM 断言。
- Hub 流式代理补齐 Responses 完成事件兜底：即使上游没有稳定发送 delta，也能从 `response.completed` 恢复最终正文与 usage，避免真实 Terra/Sol 请求偶发显示为空回答。

## 2026-08-20：课程集市与平台模型接入最终打磨

- 知识广场改为响应式课程集市，为数学分析 B1、线性代数 B1、概率论与数理统计、大学物理、数据结构和程序设计基础加入本地课程封面、资料数、更新时间与明确入口状态。
- 数学分析 B1 继续关联 25 份真实可检索资料；其余五门课程明确显示“演示入口 · 资料待补充”，不制造虚假资料或订阅数据。
- 新增 `cover_asset` 数据迁移、路径白名单、幂等 seed 更新和专用静态资源入口；修复封面 404、透明插画裁切及重复 seed 伪刷新更新时间。
- 应用、包与正式 Manifest 统一升级为 `0.9.0`；Manifest 正式声明 `platform-model-gateway` 和 `platform_optional`，已有 Hub 数据库可正常升级并建立 Agent 专属模型绑定。
- 静态资源版本提升，避免浏览器继续使用旧界面；真实浏览器验证六张封面均可访问，桌面与 390 px 移动端无横向溢出或控制台错误。
- 瀚海行 Python 112 项与 JavaScript 12 项全部通过；平台真实模型调用及三服务固定验收 10/10 轮通过。

## 2026-08-20：学生求索主题新图标

- 瀚海行正式图标更新为具象的学生、书页之舟、校徽船帆和三层知识海构图，保留现有深色界面的学院感与蓝白配色。
- 中科大校徽以暖白色印染纹样嵌入蓝色船帆，保留环形结构、书帆图案与 `1958`，不再表现为独立白色贴片。
- 图标继续输出为 `256×256` RGB PNG，并将首页、侧栏、favicon 与虚拟形象预览的缓存标识升级为 `product-logo-v2`。

## 2026-08-20：品牌文案统一

- 首页标语移除中间逗号，统一显示为“真理如瀚海求索亦行舟”。
- 主界面、输入提示、虚拟形象与预览页、OpenAPI 标题和测试中的品牌名统一为“瀚海行Agent”，确保英文 `Agent` 的首字母始终大写。

## 2026-08-20：整站视觉对齐 Campus Agent Hub

- 默认深色主题改用与 Hub 一致的深空黑、克制蓝紫强调和三级文字色；统一侧边栏、按钮、输入框、选择器、卡片、表格、引用、公式块、状态标签、弹窗与提示消息的视觉反馈。
- 问答首页强化课程知识工作台主视觉，同时保留瀚海行 Logo、小比熊虚拟形象、问答模式、模型选择、上下文用量和资料入口；知识库、知识广场、日程、设置与阅读器继续采用适合重复操作的紧凑布局。
- 保留浅色主题兼容和全部原有业务交互；PDF 150% 缩放、文本字号调整、资料阅读、日程弹窗、虚拟形象拖动与 100% 到 105% 滚轮缩放均通过真实浏览器检查。
- 修复 390 px 首页模型控件重叠、知识库标题断行和不稳定的视口字号；全站文字间距统一为 0，桌面与移动端检查页面均无横向溢出。
- 自动化验证为 Python 108 项、JavaScript 12 项全部通过；与 Hub、Demo Agent 和 Contract 合计 207 项。深浅主题主要页面已在 `1440x900` 与 `390x844` 完成截图审计，浏览器控制台无错误或警告，三服务固定验收 10/10 轮通过。

## 2026-08-18：五项核心优化中的瀚海行交付

- 瀚海行改用自托管 Inter、Noto Sans SC 和 Noto Serif SC 字体，默认深色主题及既有浅色偏好保持不变；静态资源版本同步提升，避免浏览器继续使用开发期缓存。
- 新增课程集市式知识广场：数学分析 B1 展示并复用现有 25 份真实 OCR 资料，其余五门课程清晰标记为演示知识库和“资料待补充”；幂等 seed 不覆盖用户修改，也不生成虚假文档数。
- 接入 Hub `platform_optional` 模型运行时：Featured 工作台和 Connected 调用使用服务端短期委托与单次 Grant，浏览器和瀚海行进程均不会接触用户 API Key；未从 Hub 启动时继续使用原有 Agent 配置。
- Profile 禁用、换绑、凭据或协议变化、身份切换、工作台退出及 Connected run 结束都会撤销旧委托和未消费 Grant；撤销失败不再误报成功。
- 真实浏览器分别完成 `gpt-5.6-sol`、`gpt-5.6-terra` 直接问答，并使用单份《第1-3章复习提纲》完成带第 2 页和第 10 页引用的 RAG 回答。
- 瀚海行 Python 108 项、JavaScript 12 项全部通过；与 Hub、独立 Demo Agent 和 Contract 合并计算的最终回归共 207 项通过。

## 2026-08-18：Responses / Chat Completions 双接口兼容

- `a5b5f5f` 为模型适配器增加 `responses` 与 `chat_completions` 两种 API style；Chat Completions 模式使用标准 `messages`、`max_tokens` 和 SSE `choices[].delta.content`，不向兼容服务发送 Responses 专用参数。
- Chat Completions 非流式与流式结果均沿用现有隐藏推理边界，只把最终回答传给前端；用量归一化兼容 `prompt_tokens` 与 `completion_tokens`。
- `5072610` 将 API style 接入管理员设置接口、设置页、Docker Compose 和非 Docker 启动脚本，配置保存后会重新创建模型适配器并立即生效；非法模式由接口返回 422，不再静默保存。
- 模型目录把服务端发现、且未识别为图像、音频、实时、向量、重排、转写、审核等非聊天类型的模型视为可选文本模型，通义千问、DeepSeek 等名称不再被统一禁用。
- 本地脚本支持在缺少 `.env` 时创建配置，并修复重复启动时 Demo Agent 进程匹配错误；Featured 密钥二次轮换继续由运行时文件热加载承接。
- 自动化验证为瀚海行 Python 97 项、Hub Python 26 项、第三方 Demo Python 7 项、JavaScript 31 项、Contract 5 项，共 166 项全部通过；固定三服务验收 10/10 轮成功，现有 Responses 模式 GPT-5.6 真实问答和上下文用量显示正常。Chat Completions 兼容路径已由请求载荷、非流式响应和 SSE 流式单元测试覆盖。

## 2026-08-17：Featured 工作台密钥轮换修复

- `d877092` 将 Hub client secret 的文件读取从“进程启动时一次性加载”改为“每次工作台 token 兑换前读取”，重复 bootstrap 后无需重启瀚海行。
- 直接环境变量密钥继续静态优先；运行时文件丢失或为空时返回未配置错误，不允许使用内存中的旧文件密钥。
- 新增在应用进程已经创建后把 secret 文件从旧值改为新值的回归测试，并断言下一次兑换发送的是新密钥。
- 自动化验证为瀚海行 Python 95 项、Hub Python 26 项、第三方 Demo Python 7 项、JavaScript 31 项、Contract 5 项，共 164 项；零宽限真实密钥轮换后三服务验收 10/10 轮成功。

## 2026-08-09：资料保存闭环与队友提交回归修复

- 新增“保存至个人库”完整接口：共享或订阅资料只有在当前用户具备下载权限时才可复制；保存后生成独立个人文档、页面、检索分块和 FTS 索引，并写入审计事件。
- `c75c160` 将资料写入和审计事件收进同一数据库事务；审计失败、解析失败或其他异常都会回滚文档与 FTS，并清理已复制的 PDF 和 OCR sidecar。
- OCR 资料保存或发布时同步复制经过源文件 SHA-256、页数和连续页集校验的 `.ocr.md`，确保个人副本与发布快照继续使用 OCR 文本检索；删除副本时同步删除 sidecar。
- 资料列表、首页资料选择器和知识库资料选择器共用同一勾选状态，单项选择和“全选 / 清空 / 按类型选择”都会同步刷新三个界面。
- 模型隐藏推理不再通过 JSON、SSE、聊天气泡或本地历史暴露；上游 reasoning output item 与 `reasoning_content` 均不会被当作最终回答。
- 默认最大输出恢复为 `1200` token，并支持受保护的环境变量覆盖；非法配置自动回退，最大限制为 `32000`。
- 更新前端资源版本，避免浏览器继续命中队友提交前的缓存；最终自动化验证为 Python 94 项、JavaScript 11 项全部通过，三服务固定验收 10/10 轮成功。

## 2026-08-08：默认深色主题

- 瀚海行在没有有效主题偏好时统一使用正式设计的深色界面，非法值和存储访问异常均回退深色。
- 用户主动切换浅色后仍会保存并在刷新时恢复，不强制覆盖个人选择。
- 主题核心在 CSS 加载前执行，避免首屏出现错误主题；新增可执行测试覆盖默认值、有效偏好、非法值和加载顺序。

## 2026-08-06：Hub 统一聊天故障收口

- 修复 Hub 重启或紧急轮换 Ed25519 密钥后，瀚海行与第三方 Demo Agent 因缓存同一 `kid` 的旧 JWKS 而持续返回 401 的问题；签名失败时只清空缓存、重新拉取 JWKS 并重试一次，鉴权、issuer、audience、scope 和 token 有效期校验保持不变。
- Hub 统一聊天改为单请求运行：发送期间禁用发送按钮，取消只作用于当前请求，避免多个并发 run 相互覆盖状态。
- `RUN_ERROR`、主动取消和异常 EOF 统一收尾当前占位消息，不再残留“正在等待 Agent 响应…”；错误卡片重试绑定失败当轮的 Agent 与原问题，并在重试前移除旧卡片。
- 补齐 SSE 末尾残余 buffer 处理和 AG-UI 错误归一化，避免最后一帧没有空行分隔时丢失终止事件。
- 为 `app.js` 与 `hub-core.js` 同步增加静态资源版本，修复浏览器缓存旧模块导致 `errorFromAguiEvent` 导入失败、广场主区域空白的问题，并增加版本化依赖回归检查。
- 自动化验证通过：瀚海行 Python 87 项、Hub Python 26 项、第三方 Demo Python 7 项、Hub JavaScript 9 项；真实浏览器验证正常回答、发送锁定、取消、失败与重试均不残留等待气泡；固定三服务验收连续 10/10 轮成功。

## 2026-08-06：Campus Agent Hub 深夜智能舱视觉改版

- Agent 广场改为沉浸式深色门户：首屏围绕校园问题入口组织信息，保留搜索、筛选、Agent 卡片、开发者接入、统一聊天和管理审核等原有功能。
- 新增可复现的 Canvas 星空：星点集中在标题附近并向两侧稀疏分布，使用固定种子、椭圆高斯采样和值噪声形成自然星团，支持非同步闪烁和轻微漂移。
- 鼠标移动即可拨开深色迷雾并唤醒局部星辉，无需点击或拖动；背景层不参与命中测试且首屏禁止误选文字，输入控件仍可正常选择和编辑。
- 为减少动态效果、浅色主题和粗指针设备提供降级；离开广场或切换主题时销毁 Canvas、动画帧和事件监听，避免重复挂载与后台消耗。
- 移动端重排聊天输入区，文本框独占一行、操作按钮下置；在 `1440×900` 与 `390×844` 视口验收首页、聊天、开发者接入和管理审核页，均无横向溢出或控制台错误。
- Hub 静态文件白名单增加 `starfield.js` 并补充回归测试；星空生成与交互逻辑新增独立 JavaScript 测试。

## 2026-08-06：Campus Agent Hub 治理与交付验收

- Hub 升级到 `v0.2.0`：Manifest 提交后自动生成版本级验收证据，管理员批准前必须通过 URL、启动页、健康与聊天 Contract 检查。
- Gateway 增加请求/响应大小限制、SQLite 持久限流、连续健康失败准入、严格 AG-UI 生命周期校验、客户端取消状态和 usage 元数据审计。
- Featured client secret 支持轮换与撤销；版本切换后旧授权码立即失效；Ed25519 私钥持久化到忽略的运行时目录，修复 Hub 重启后 Agent 因 JWKS 缓存旧密钥而返回 401 的问题。
- 外部 Agent 图标改由 Hub 下载并校验 MIME、大小和文件签名，浏览器只加载平台安全副本。
- 新增固定比赛验收脚本，真实调用独立 `simple-chat` Demo、瀚海行原生 AG-UI 和 Featured 工作台，并验证授权码防重放；2026-08-06 实测 10/10 轮成功。
- 自动化验证更新为 Hub Python 26 项、瀚海行 Python 86 项、Demo Agent Python 6 项、两组 JavaScript 各 8 项和 Contract 5 项，共 139 项。

## 2026-08-05：Campus Agent Hub 三服务闭环

- 新增独立 Campus Agent Hub：实现版本化 Manifest、注册审核、暂停恢复与回滚、健康检查、审计日志、应用广场、统一聊天和开发者接入页面；Hub 使用自己的 SQLite 数据库，不导入瀚海行业务代码。
- 冻结 `USTC-Agent-Hub Contract v1`：统一 Manifest、Health 和 `simple-chat` Schema；标准 Agent 使用 `@ag-ui/core@0.0.57` 的 `RunAgentInput` 与 SSE 事件，轻量 Agent 通过通用适配器接入。
- 瀚海行新增独立 Hub 适配端点：使用 EdDSA JWT、`kid`、JWKS 和请求 ID 校验 Gateway 调用；Featured 完整工作台通过 60 秒一次性授权码与 `client_secret_basic` 建立本地会话。
- 新增独立校园助手 Demo Agent，证明第二个 Agent 只需注册 Manifest 与标准端点即可接入，Gateway 不增加 Agent 专属业务分支。
- 新增 Docker Compose 演示编排：自动启动三项服务、注册并审核两个 Agent、生成仅存在于运行时卷的 Featured 凭据，并导入 25 份数学分析 B1 OCR 资料；模型凭据只从未跟踪的本地 `.env` 注入。
- 修复 Hub 深层聊天地址刷新后脚本和样式从错误相对路径加载的问题，并修复 Featured 工作台在异步授权后被浏览器弹窗策略拦截的问题。
- 安全审计后收紧默认边界：Hub 仅在显式 Demo 模式下接受演示身份头；第三方域名解析到私网地址时拒绝访问；两个 Agent 都校验 JWT scope，瀚海行 Hub 端点默认要求鉴权；Featured secret 改为权限收紧的运行时文件注入，不进入命令行或普通环境变量。
- 自动化验证覆盖 Hub 后端 10 项、Hub 前端 8 项、瀚海行 Python 86 项、瀚海行 JavaScript 8 项、Demo Agent 6 项和 Contract 5 项；真实浏览器另外验证两个 Agent 对话、Featured 工作台授权、开发者表单及桌面/手机布局。

## 2026-08-05：PetPresence 小比熊虚拟形象

- 从团队自有的 MIT 开源项目 `xytsakura/PetPresence-public` 引入经过公开资源审计的小比熊 Demo：六个透明 WebM 动作和一张播放失败海报均保留来源提交与许可证。
- 虚拟形象设置新增“小比熊”，同时保留原有男生、女生形象；新用户默认使用小比熊，既有用户保存的选择继续生效。
- 小比熊的待机、警觉、玩耍和进食动作分别接入 Agent 的待机、模型思考、点击互动和陪伴学习状态；非循环动作结束后恢复待机。
- 支持把鼠标悬停在角色上直接滚轮缩放，沿用现有用户级缩放持久化、滑杆、按钮、拖动和输入区边界保护。
- 在减少动态效果模式下不自动播放视频，只显示首帧/海报；透明 WebM 无法播放时保留小比熊海报回退。
- 自动化验证为 Python `78` 项、JavaScript `8` 项全部通过，并单独执行前端语法检查和 WebM 静态资源校验。

## 2026-08-05：虚拟形象悬浮布局

- 虚拟形象脱离首页双列网格，改为右下角固定悬浮；聊天区恢复完整单列宽度，不再为角色永久占用右侧空间。
- 保留默认开启、拖动、键盘移动、缩放、气泡和快捷功能；拖动边界只计算当前可见控件，打开控件时自动重新约束位置。
- 分别调整桌面、平板和手机尺寸及底部安全距离；移动端不再保留角色占位行，也不通过压缩输入框宽度规避遮挡。
- 撤销同批提交中与本功能无关的模型默认值、设置控件和演示用户权限变化，维持此前问答、RAG 和管理员配置边界。
- 自动化验证为 Python `77` 项、JavaScript `8` 项全部通过；真实浏览器覆盖 `1440×900` 与 `390×844` 视口及拖动操作。

## 2026-08-04：主问答与独立分支流式输出

- 新增 POST + SSE 流式端点；主问答和 GPT-5.6 独立解释分支都直接转发 Responses-compatible 上游的真实文本增量。
- 保留原有 JSON 端点，设置测试和已有调用方无需同步迁移。
- 流式正文实时追加，知识库引用、模型信息和 token usage 在正常完成并经过服务端校验后统一确认。
- 处理 `response.completed`、`response.incomplete`、`response.failed`、`error` 和异常 EOF；中途失败保留部分正文但不把它写成正常历史。
- 用户切换身份、切换或清空会话时主动取消旧流；旧连接不得写入新身份界面和历史。
- 新增无 npm 依赖的 SSE 分块解析测试，并保留权限前置、引用白名单、错误脱敏和旧 JSON 接口回归测试。

## 2026-08-04：免费订阅知识库广场

- 新增“知识广场”一级视图，支持浏览公开课程知识库、查看资料策略、订阅、取消订阅和进入独立只读知识空间。
- 个人空间可明确选择资料投稿；服务端复制为不可变快照，个人原文后续修改或删除不会改变已提交版本，未勾选资料不会进入快照。
- 管理员可以逐份调整 `use_in_rag`、`can_preview`、`can_download`，并批准、要求修改或拒绝；作者不能审批自己的投稿。
- 新版批准后所有订阅者自动切换，旧版保留用于审计和回滚；支持暂停、恢复和下架，所有旧文档 ID 都按当前版本和当前状态即时重新鉴权。
- 订阅关系与邀请制 `memberships` 分离；FTS 检索先获得当前用户的授权文档 ID，再只在授权集合内搜索。
- 未订阅登录用户只能预览明确允许预览的当前公开资料；下载与 RAG 必须同时满足有效订阅和逐份策略。作者与管理员拥有独立审阅权限，不伪装成订阅者。
- 新增普通访客 `demo-c`，与管理员 `demo-a`、投稿作者 `demo-b` 共同覆盖三种角色。
- 自动化测试覆盖投稿、审核、订阅权限、策略矩阵、取消订阅、暂停恢复、换版、回滚、基础版本冲突、旧 ID 失效，以及多文档投稿失败后的数据库、FTS 和文件清理。

## 2026-08-04：多段引用与 GPT-5.6 独立解释分支

- 助手回答支持文本选择工具条：可把最多 8 段、合计不超过 4000 字的片段加入下一轮主对话引用篮，也可以直接在源回答下方打开独立解释分支。
- 主对话引用以可删除、可上下调整顺序的结构化条目展示，不修改用户输入正文；提交时通过 `context_references` 传给服务端，并在模型提示中与用户指令隔离。
- 每条助手消息使用稳定消息 ID；独立分支包含源回答、所选片段、多轮问题与回答，支持折叠、重新打开和把分支回答手动回引主对话。
- 分支随原会话保存在浏览器 `localStorage`，恢复历史会话后继续可用；尚不提供跨设备同步。
- 新增 `/api/branch-query`：固定使用服务端 `COURSE_AGENT_BRANCH_LLM_MODEL`（默认 `gpt-5.6-sol`）和 direct 模式，不接受资料范围或浏览器自选模型，始终返回空引用列表。
- 对引用片段、完整源回答、问题和分支历史增加结构与长度校验；提示词明确把引用视为不可信数据，防止其中的指令覆盖系统边界。

## 2026-08-03：数学分析 B1 全量 OCR 回填

- 新增 DeepSeek-OCR-2 Markdown OCR sidecar 管线：逐页 PDF 渲染、多 endpoint 并行、页级缓存、断点续跑、有限重试、进度输出和原子发布。
- PDF 解析器优先使用哈希、页数和页面完整性均校验通过的 `<原文件名>.ocr.md`；无效或缺失 sidecar 自动回退到 PyMuPDF。
- 完成 25 份唯一 PDF、510 页的全量回填并重建 SQLite FTS5：500 页可检索、10 个空白页不入索引、686 个检索分块。
- 原 PDF 继续作为文件下载、页面预览和引用来源；OCR Markdown 仅替换 RAG 检索文本，因此引用仍保留原文件名和页码。
- 清理 DeepSeek OCR 定位标记，索引中未出现 `<|ref|>` 或 `<|det|>`；新增 sidecar 哈希失效、页面不完整、空白页和引用页码回归测试。

## 2026-07-31：YSY 与王梦莲版本整合后的迭代

> 本节由 2026-08-01 补记。Git 历史采用线性整合，没有单独的 merge commit：YSY 的 `2d95552`、`0a7d150` 先进入主线，王梦莲的 `fe98e05` 随后建立在其上。因此，本节以 `fe98e05` 作为双方版本整合完成的基线，统计 `fe98e05..11e44a1` 的 11 次后续提交。

### 整合基线中已经保留的能力

以下能力来自双方版本整合本身，不属于本节所说的“合并后新增”：

- **YSY 主线**：多轮对话与会话历史、直接问答/知识检索切换、按资料勾选检索范围、当前模型显示、四面板宽度拖拽与持久化、Python 3.9 兼容和后端历史上下文。
- **王梦莲前端**：瀚海行品牌与 Logo、深浅主题、日程表、个人信息与头像裁剪、男女虚拟形象及动作、虚拟形象快捷功能、天气接口、功能开关、响应式布局和相关专项测试。

### 模型调用恢复与兼容

- **恢复 Responses API 调用链路**（`ba67589`）：重新按 Responses-compatible 协议发送模型请求，兼容 `output_text` 和结构化 `output` 文本提取，保留多轮上下文与知识库引用。
- **错误降级与密钥脱敏**：模型网络错误、HTTP 错误、空响应和缺失引用会返回可解释的降级结果；错误信息不会暴露 API Key 或 Bearer 凭据。
- **回归测试补强**：增加 Responses 请求体、多轮历史、引用、异常响应、凭据脱敏和降级行为测试。

### 个性化 Agent 回答

- **瀚海行 Agent 身份提示词**（`de0df47`）：直接问答和知识库问答都使用统一产品身份，不再把助手只描述为底层通用模型。
- **语气设置**：支持“亲和”和“务实”。
- **回答详略**：支持“简洁”“均衡”“详细”，只控制最终答案篇幅，不控制模型思考强度。
- **个人补充要求**：用户可填写自定义偏好；该内容作为独立用户上下文传给模型，不拼入 system prompt，不能覆盖真实性、权限或引用规则。
- **按用户保存**：个人回答偏好保存在当前用户的浏览器空间中，切换身份时同步切换。

### 多模型、思考强度与上下文用量

- **模型自动发现**（`16fad69`）：管理员可基于已保存的 Base URL 和 API Key 查询服务暴露的模型，并区分可用文本模型与不可用模型。
- **会话级模型选择**：每个会话可选择不同模型；恢复历史会话时同时恢复该会话的模型设置。
- **会话级思考强度**：支持“快速、均衡、深度、极深、最高（高级）”，并按模型能力决定是否显示与传递该参数。
- **上下文用量圆环**：解析 Responses API 的输入、输出、推理、缓存和总 token；结合模型上下文窗口显示本轮使用比例。
- **安全边界**：API Key 只保存在服务端，不返回浏览器；只有管理员身份能保存模型服务配置和执行模型发现；外部 Base URL 默认要求 HTTPS，本机地址需显式开启。
- **回退与缓存**：模型目录不可用时可回退到已保存模型或缓存结果，避免设置页完全失效。

### 引用原文与资料阅读器

- **引用可点击**（`3c8c159`）：回答中的 `[S1]` 等标记和引用卡片可以直接打开对应资料与页码。
- **原始页面/提取文本双视图**：阅读器支持查看后端渲染的 PDF 原页，也可切换到该页的提取文本。
- **翻页与外部打开**：可在阅读器内前后翻页，也可在新标签页打开原始 PDF。
- **权限与缓存控制**：文件、页文本和页图片接口都会重新校验当前用户权限；私有内容使用 `private, no-store`，页面渲染带并发限制和有界缓存。
- **桌面与移动端**：桌面端为可调整宽度的右侧阅读器；移动端切换为全屏对话框，并处理焦点恢复和背景不可交互状态。

### 阅读缩放与 OCR 待办

- **PDF 页面缩放**（`208fc36`）：支持 `50%–250%` 缩放、重置、双向滚动和 `Ctrl + 滚轮`；缩放比例保存在浏览器中。
- **文本字号调节**：提取文本支持 `12–28px` 字号调整和重置，设置与 PDF 缩放分别保存。
- **移动端平移**：放大的 PDF 页面只在阅读器内部滚动，不撑开整个页面。
- **统一 OCR 链路待办**：新增 [OCR_PIPELINE_TODO.md](../../docs/OCR_PIPELINE_TODO.md)，记录扫描 PDF、图片资料和手写体的 OCR 服务部署、API、重新切块与建索引、质量验收和权限安全要求。OCR 服务目前仍是待办，不能描述为已经上线。

### Markdown 表格与模型输出契约

- **表格渲染修复**（`11e44a1`）：前端可把标准 Markdown 表格渲染为真正的表头、行和单元格；兼容模型偶尔在表格行之间插入空行的情况。
- **移动端表格滚动**：宽表格在自身容器内横向滚动，不造成页面级横向溢出。
- **安全与失败策略**：单元格内容先转义再渲染，避免模型输出 HTML 形成 XSS；列数异常的行回退为普通文本，不静默截断内容。
- **统一 system prompt 输出契约**：直接问答和知识库问答都明确告知模型当前前端支持标题、段落、粗体、引用、列表、Markdown 表格和 KaTeX；不要求模型输出前端尚未支持的 HTML、Mermaid 或 ASCII 伪图表。

### 文档与验证

- 更新课程 Agent README、部署审计指南和 `.env.example`，补充管理员、模型发现、多模型、思考强度和上下文用量配置。
- 新增模型目录、Responses usage、权限、提示词隔离、文档原文访问、阅读器、缩放和 Markdown 表格相关回归断言。
- `5a88405` 统一前端测试文件的行尾格式，减少跨平台开发时的无效差异。
- 截至提交 `11e44a1`，课程 Agent 测试集实测为 **42 项通过**；JavaScript 语法检查、桌面/移动端浏览器验证和独立审计均通过。

### 提交索引

| 提交 | 类型 | 主要内容 |
| --- | --- | --- |
| `5a88405` | 维护 | 统一前端测试文件行尾格式 |
| `5ff2a16` | 文档 | Responses API 回归修复说明 |
| `ba67589` | 修复 | 恢复 Responses-compatible 模型调用与异常处理 |
| `de0df47` | 功能 | 个性化身份、语气、详略和用户补充偏好 |
| `e20564e`、`425fb1e`、`92455e9` | 文档 | 多模型、上下文用量、模型发现安全和思考强度规则 |
| `16fad69` | 功能 | 模型发现、会话级模型/思考强度和上下文用量 |
| `3c8c159` | 功能 | 可点击引用和原文阅读器 |
| `208fc36` | 功能 | PDF 缩放、文本字号和 OCR 待办 |
| `11e44a1` | 修复 | Markdown 表格渲染与模型输出契约 |

## 2026-07-26

### 新增
- **多轮对话（同一窗口连续追问）**：首页问答改为「可滚动会话区 + 固定底部输入框」的聊天式布局，新的回答累加在输入框上方。
- **开启新对话**：仅当用户点击「开启新对话」时才清空窗口并开始一个新会话；在此之前连续追问会保留完整上下文。
- **会话历史操作菜单**：左侧每条历史记录新增三点操作菜单，支持：
  - 置顶 / 取消置顶（置顶项自动排到顶部并显示绿色标记）；
  - 重命名（弹窗输入新名称，长度上限 2000 字）；
  - 删除（二次确认后移除）。
- **点击历史恢复完整会话**：点击某条历史记录可回到当时的对话，完整还原用户问题与助手回答（含 Markdown 与公式渲染），并恢复对应的问答模式。
- **会话持久化**：历史记录（标题、预览、置顶状态、模式、完整消息流）保存到浏览器 `localStorage`，刷新后保留；当前正在查看的会话会高亮。

### 后端
- `QueryRequest` 新增 `messages` 字段（`list[ChatMessage]`），用于接收前端拼接的多轮上下文。
- LLM 适配器 `generate_direct` / `generate` 支持 `history` 参数，按 `system + 历史 + 当前问题` 构造请求。
- 新增 `_sanitize_history()`：过滤非法角色、空内容，并对超长历史做截断。

### 修复
- 修复点击会话历史恢复时因「遍历数组的同时向同一数组追加元素」导致的**死循环卡死**；改为基于快照遍历、并新增只渲染气泡不修改状态的 `appendHomeMessageBubble()`。
- 修复恢复会话时只渲染用户问题、丢失助手回答的问题，现在同时还原助手回答内容。

### 测试
- 新增 pytest 回归用例：验证多轮对话历史按序拼接、非法历史条目被正确过滤；全部用例通过。

### 新增（首页问答模式切换）
- **直接问答 / 知识检索 模式切换**：首页底部输入框左下角新增两个模式按钮。
  - 直接问答：使用默认通用大模型，不调用知识库检索。
  - 知识检索：基于当前知识库资料进行回答。
- **知识检索资料选择面板**：切换到知识检索后，输入框内展开资料选择面板，支持按资料类别选择、选择全部资料、清空、以及单独勾选具体资料；主页与知识库页面的选择状态实时同步。
- **当前模型显示**：对话框右侧展示当前使用的模型名称（如 `gpt-5.6-sol`），登录或保存模型设置后自动刷新；请求返回后也会以后端实际模型名更新。
- **历史会话保留模式**：恢复历史会话时回到该会话最后使用的问答模式。

### 修复
- 修复 pytest 在 Python 3.9 环境下收集阶段失败的问题：`QueryRequest` 的 `scope` 与 `space_id` 字段由 PEP 604 联合注解（`Literal[...] | None`、`str | None`）改为 `typing.Optional[...]`，使 Pydantic 在 Python 3.9 可正常求值；其余 `@dataclass` 字段与函数签名因已使用 `from __future__ import annotations` 不受影响。

### 新增（四面板边界可拉伸）
- **侧边栏 / 知识库空间列表 / 文档列表 / 问答栏** 之间的边界均可拖拽调整宽度，带最大最小限制：
  - 侧边栏：200–400px（默认 260px）
  - 知识库空间列表：200–380px（默认 260px）
  - 问答栏：320–720px（默认 420px）
- **持久化**：拖拽后的宽度会保存到 `localStorage`，刷新或重新登录后保留。
- **键盘可访问**：聚焦分隔条后可用 `← / →` 微调（`Shift` 加大步长）。
- **触屏支持**：兼容移动设备触屏拖拽。
- **拖拽过程**：拖动时全局锁定文本选择与光标为 `col-resize`，避免误选文字。
- **响应式**：窄屏下分隔条按既有断点自动隐藏，避免布局冲突。

### 当时的改动文件
- `course_agent/web/index.html`：首页结构调整（移除快捷卡片，改为会话区 + 底部输入框 + 新对话按钮）；新增模式切换按钮、知识检索资料选择面板与当前模型显示节点；在侧边栏与主区、空间列表与文档列表、文档列表与问答栏之间插入 3 条可拖拽分隔条。
- `course_agent/web/styles.css`：聊天气泡、会话历史项、操作菜单与置顶/高亮样式；模式切换、资料选择面板、当前模型标识样式；面板宽度 CSS 变量（`--library-spaces-width`、`--library-chat-width`、`--resize-handle-size`）、分隔条可视化与拖拽光标/选中文本锁定、`.library-layout` 网格改用变量化列宽、各面板 `min-width: 0`、响应式断点中同步隐藏分隔条。
- `course_agent/web/app.js`：多轮状态管理、会话历史增删改查、点击恢复、模式记忆；模式状态管理、资料选择同步、当前模型加载与刷新、历史模式恢复；面板分隔条拖拽/键盘/触屏交互、`clamp()` 边界限制、`localStorage` 持久化（`course-agent:panel-width:*`）。
- `course_agent/main.py`：`QueryRequest` 增加 `messages` 字段并透传 `history`；`QueryRequest.scope` / `space_id` 字段改为 `Optional[...]`（兼容 Python 3.9），并在请求返回后用实际模型名刷新前端展示。
- `course_agent/llm.py`：`generate_direct` / `generate` 支持 `history`，新增 `_sanitize_history()`。
- `tests/test_llm.py`：多轮历史拼接与过滤测试。
