# 更新日志

本文件记录整个 `AI for better life In ustc` 项目的平台级变化，方便队员直接从 GitHub 了解每次主要提交做了什么。课程资料 Agent 的更细功能记录继续维护在 [apps/course-agent/CHANGELOG.md](./apps/course-agent/CHANGELOG.md)。

## 维护规则

- 所有影响交付的功能、修复、重构、测试和重要文档提交，必须在提交或紧随其后的日志提交中同步更新本文件；涉及具体应用时还必须同步更新该应用自己的 `CHANGELOG.md`。
- 推送前必须检查本轮产品变化是否已有日志条目。纯格式整理、生成文件以及只维护更新日志的提交不递归记录，避免产生没有产品信息的日志链。
- 每条记录包含日期、短提交号、提交类型和用户可感知的变化，不粘贴大段代码差异。
- 一次提交尽量只处理一个主题；提交信息使用 `feat`、`fix`、`docs`、`refactor`、`test` 或 `chore` 等清晰前缀。
- 尚未实现的想法放入设计文档或待办，不写成已经交付的更新。

## 2026-09-01

### `merge/fix`：合并队友视觉更新并收口跨平台部署

- 合并 Hub 设计 token、首页、Agent 广场、筛选区、模型配置中心、对话存档删除和 Safari 静态资源缓存修复，保留本地比赛《设计文档》《作品简介》及双方更新日志。
- 修复知识广场 seed 自动订阅的异常路径：无效 Manifest 项只记录失败，不再访问未初始化变量、沿用上一项状态或误订阅其他课程；重复 seed 继续保持幂等。
- 将 `/`、`/hub`、Hub 深链和顶层 JS/CSS 统一为 `no-store/no-cache`，避免从常用根入口仍命中旧版前端。
- Windows 原生入口补齐 `.env` 安全加载、模型主密钥生成、课程 Agent 模块路径、数据库初始化、25 份资料导入和知识广场 seed，并支持隔离运行目录；Shell、PowerShell 和 Docker 三条入口保持相同初始化顺序。
- Docker PowerShell 入口新增 Docker/Compose 前置检查、配置校验和后台部署就绪门禁；只有三项服务健康且四个必需演示 Agent 均 active 后才报告成功，复用含额外 Agent 的旧 volume 也不会被误判失败。
- 真实浏览器验收发现并修复 Agent 广场宽屏三列回归，恢复瀚海行位于左上角的两两排列；移动端保持单列。

## 2026-09-02

### `fix(course-agent)`：修复本地启动覆盖模型地址导致模型不可用

- Windows 本地启动入口不再把根目录模板 `.env` 中的空值写入进程环境，避免空的 `COURSE_AGENT_LLM_BASE_URL` 覆盖瀚海行自己的应用配置。
- 重启后的瀚海行可正确读取本地模型 API 配置，健康检查恢复 `llm_configured: true`，并通过 `gpt-5.6-sol` 完成流式问答。

### `docs`：补充 107 杯 Demo 剧本的需求路由流程

- 将 `107杯.docx` 提取并整理为可编辑的 `107杯.md`，保留原有开场、平台、瀚海行和结尾讲解内容。
- 新增需求分析路由录制段落：以“复习数学分析”演示路由到瀚海行，以“签字盖章”演示路由到校园公共服务 Agent，并说明普通问答不触发复杂路由。
- 为每个路由节点补充实际操作提示，覆盖模式选择、推荐卡片、直达 Agent 和 Future Work Demo 说明。
- 按 5 分钟录制约束制作 A 版保守精简稿：合并重复说明，保留完整平台定位、两个路由案例、瀚海行资料检索和结尾展望；将身份权限、普通问答回退、知识空间分类与日程表标为可选口播，并增加时长控制建议。
- 将完整 A 版 Markdown 剧本排版为可直接在 WPS 打开的 `107杯-A版口播稿.docx`，保留所有口播与操作提示，并加入页眉、页脚、标题层级和提示块样式。

## 2026-08-31

### `docs`：新增并审查比赛提交材料

- 审查比赛提交材料，校正《设计文档》的版本说明：明确 `23305bf` 为实现基线、`4524c5d` 为材料提交，并记录已验证的本地文档链接别名。
- 新增比赛提交材料《设计文档》和《作品简介》，统一以当前 `main` 的平台化方案为主线，以瀚海行为第一方标杆 Agent。
- 文档补充 Contract、Registry、Review、Gateway、首页双模式路由、模型网关、OCR/选择性 RAG、权限边界、部署方式和实际验收结果。
- 明确评课社区与校园公共服务为 Future Work Demo，避免将尚未接入真实数据源的能力表述为已完成。

### `fix(course-agent)`：修复本地启动时知识广场为空

- 本地启动脚本 `start-local.sh` 遗漏了 `deploy/compose.yaml` 中的 `seed-marketplace` 步骤，导致从 Docker 切换到本地启动后知识广场为空（个人与共享空间的 26 份资料未受影响）。
- 已补齐该步骤并加注释说明必须与 compose 保持一致；命令幂等，重复执行安全。
- 恢复后知识广场 6 门课程：数学分析 B1 期末复习库（25 份文档）+ 5 门演示占位库。
- 修复资料回答无依据：知识广场 seeded 后默认用户并未订阅公开库，`accessible_document_ids` 返回空，导致检索后显示“没有找到足够依据”。已在 `seed-marketplace` 中对 `demo_kind: real` 的课程自动写入作者订阅。

### `refactor(hub)`：前端设计系统统一与首页、广场视觉重构

- Hub 前端样式此前存在四层叠加定义，两层失效主题被后续规则整体覆盖，实际生效的圆角只有 4/6/8px 且卡片无投影。已合并为单一 token 源，新增圆角、阴影、间距、字号四级标尺并全量替换硬编码数值，硬编码颜色改由主题变量派生。
- 修复两处从未生效的样式：Agent 卡片 hover 上浮（选择器误写为 `.agent-card`）和侧边导航当前页高亮（被后续规则覆盖）；同时删除 `.portal-search`、`.filter-panel`、`.hub-tabs` 等约 130 行死代码。
- 首页：英文大标题降级为字距标签，主标题统一字体并改为响应式字号，品牌区与对话区对齐到同一列，模型状态条由悬浮胶囊改为脚注，输入框新增聚焦环。
- Agent 广场：卡片信息由 6 层精简为 4 层，健康状态改为色点，底栏吸底对齐，网格改为自适应列数；修复首页快捷卡片因 `flex-direction` 被覆盖而竖向堆叠的问题。
- 筛选区由折叠面板改为常驻三行标签结构，标签换行显示并在超过 8 个时折叠为 `+N`，选中项始终可见。
- 根据用户反馈，进一步移除广场卡片的能力小标签列表，使卡片更加简洁紧凑。
- 简化多模型配置中心（/hub/settings）：精简页面与表单文案，缩短字段标签与按钮，合并相关字段为两列布局。
- 本地启动脚本启用 Model Profile 功能（自动生成加密主密钥）并新增 provider 白名单；修复脚本内注释行导致环境变量被静默丢弃的问题。
- 修复模型配置编辑保存失败：前端原本每次 PATCH 都回传 `base_url`，触发后端「修改 Base URL 必须提供 API Key」校验；改为差分提交，仅发送变化字段，并提前提示用户。
- 改进前端错误提示，支持 FastAPI 数组型校验错误并映射 `api_key_required_for_base_url_change`。
- 完成 DeepSeek 配置端到端实测：Profile 创建、连接测试、模型发现、全局与 Agent 绑定、首页助手双模式、Agent 委派链路全部打通，重启后配置持久化；明确 Agent 委派需单独绑定，全局默认不会级联。
- 修复模型配置页因静态资源缺少 `Cache-Control` 导致 Safari 缓存旧版 `app.js` 而卡在骨架屏的问题；为 SPA 与静态资源统一设置 no-store 缓存头，并在 HTML 中增加 meta 缓存控制。
- 根据用户反馈移除首页模型状态条（显示 API Key 掩码、全局默认、瀚海行绑定等信息的区域），首页不再请求模型配置，更简洁。
- 首页左侧「对话存档」列表的每条历史对话新增删除按钮，hover 时显示，可单条删除；删除当前对话则自动新建空对话。
- 恢复误删的多模型配置进度概览（14 条配套 CSS 仍在，属不完整删除），并修复一处因硬编码缓存版本号而必然失败的测试；前端 JavaScript 测试 39 项全部通过。
- 详细记录见 [apps/hub/CHANGELOG.md](./apps/hub/CHANGELOG.md)。

## 2026-08-22

- 修复需求分析路由对明确演示话术的随机漏判：期末/课程复习需求优先匹配瀚海行，签字盖章与办事地点需求优先匹配校园公共服务 Agent。
- 高置信匹配仍与运行时 active Registry 求交集，不会推荐离线、未审核或不可见 Agent；模糊需求继续由模型语义路由。
- 收紧“签字/盖章”规则的办事语义，避免把“公章图案怎么画”“签字怎么写好看”等内容误路由为校园公共服务。
- 新增模型错误否决、普通考试问答不过度匹配、自然语言反例和 Agent 离线不绕过校验的回归测试。
- 最终验收：三组核心演示话术各重复 10 次均正确命中；Hub Python 59 项、前端 JavaScript 39 项全部通过；三服务固定验收 3/3 通过，真实浏览器确认两类推荐卡片与瀚海行、校园公共服务两个跳转入口正常且无控制台错误。

## 2026-08-21

- Hub 首页重新拆分为“普通对话”和“需求分析路由”两个明确模式：前者不加载路由资料，后者每轮读取路由 Skill、Agent 功能表并与 active Registry 求交集。
- 两个模式统一使用真实 SSE 流式输出与 `home.completed` 收口；路由命中时返回受控 Agent 推荐卡片，未命中、超时或解析失败时回退普通回答。
- 首页存档升级为“演示身份 + 模式”双重隔离，支持模式内新建和回溯；旧记录按是否含 Agent 推荐卡片安全迁移。
- 修复本地演示只启动 Hub 导致 Agent 广场缺少两个 Future Work Demo、瀚海行与校园助手显示离线且无法进入的问题。
- 新增 `deploy/run-demo-local.ps1`：无 Docker 环境下完整启动 Hub、瀚海行和共享 Demo 服务，自动注册、检查、审核并健康确认四个 Agent；共享 Demo 服务显式接受三个注册身份的 JWT audience。
- 修复本机瀚海行虚拟环境中损坏的 `cryptography` 安装，恢复 Ed25519 Hub JWT 校验；四个 Agent 的 Contract 检查现均通过。
- 优化 Hub 首页 `auto` 模式的本地分流：对“你好”、普通知识问答、解释概念等不需要专门 Agent 的请求，直接进入普通流式回答，不再先走需求路由模型。
- 保留课程资料、复习、评课、签字盖章、办事地点等明确需求的路由能力。
- 补充并通过 `apps/hub/tests/test_home_assistant.py` 回归测试。


### `fix(hub)`：统一首页对话改为真实流式输出

- Hub 首页 `auto` 对话改为 SSE 增量输出，模型正文到达后立即逐字显示，不再等待完整回答生成后一次性出现。
- 普通回答与需求路由改为并行链路：回答使用短提示词优先流式返回，路由旁路只读取受控 Agent 清单；推荐完成后以独立事件追加到同一条回答。
- 路由超时或失败不会丢弃已经生成的普通回答；推荐 `agent_id` 继续由服务端与 active Registry 二次校验，前端不接受模型生成 URL。
- 补充流式增量、推荐事件、完整结束事件、异常 EOF、取消读取和缺少模型绑定测试，并更新浏览器模块缓存版本。

## 2026-08-20

### `feat(hub, course-agent)`：统一首页对话与知识广场封面池

- Hub 首页改为单一全屏对话入口：初始状态只保留品牌英文和中文标识，开始对话后切换为无边框全屏会话，不再显示封闭的黑色对话框。
- 即时回答与 Agent 需求路由合并为统一 `auto` 模式；模型可以直接回答，也可以在匹配到已注册 Agent 时展示安全的直达入口。
- 瀚海行知识广场新增预生成封面池；新知识库审核发布时按已使用次数与稳定随机种子分配透明插画、主题色和图标，避免新库落入难看的默认占位符。
- 保留原有 `instant` / `route` 接口兼容性，并补充统一对话与封面分配回归测试。

### `fix(hub)`：优化 Agent 广场卡片布局

- Agent 广场及首页快捷入口改为桌面端两列网格，每行展示两个卡片，避免四卡片横向拥挤。
- Featured 的瀚海行稳定置于列表第一位，确保在广场和首页快捷入口均位于左上角。
- 保留移动端单列布局，不改变筛选、进入 Agent 和其他业务交互。

### `feat(hub)`：首页即时对话与受控 Agent 需求路由

- Hub 首页由静态搜索外观升级为可用的双模式对话区：“即时对话”使用当前用户配置的全局默认模型进行流式回答，“需求路由”根据用户目标推荐已注册并上线的校园 Agent。
- 新增产品内路由 Skill 与 Agent 功能检索表；模型只输出结构化候选 `agent_id` 和推荐理由，服务端再与运行时 active Registry 取交集，伪造、下线或未登记 Agent 均不会生成入口。
- 推荐卡片分别展示 Agent 简介和本轮推荐理由，并复用现有 Agent 激活与 Featured 一次性授权流程；模型输出的 URL、token 或凭据不会用于跳转。
- 对话历史按演示身份和模式隔离并限制长度，支持真流式增量、取消、错误映射与缺少全局模型配置时的安全提示；API Key 始终只在服务端解密使用。
- 当前 README、产品文档、Hub 权威规格和协作经验已同步新边界：路由助手只负责发现推荐，用户点击后才进入 Agent，Gateway 仍为确定性代理，Agent 之间仍不互调或递归委派。
- 瀚海行模型 API Key 输入补充浏览器自动填充语义，消除真实浏览器审计中的密码字段警告。

### `feat(hub)`：扩充评课社区与校园公共服务 Future Work Demo

- 新增“评课社区 Agent”和“校园公共服务 Agent”两个 Connected Demo，支持广场进入 Hub 统一聊天。
- 两个 Demo 使用固定示例回答并明确标注 Future Work，不宣称已经接入评课社区或真实校园服务数据。
- 扩充 Agent 路由 Skill 与功能检索表，支持课程评价、教师评价、选课建议、签字盖章、行政窗口和楼宇位置等需求匹配。
- Demo bootstrap、Contract 示例、JWT audience 校验、自动化测试和部署 Compose 已同步更新。

### `fix`：透明品牌图标与模型演示闭环

- 瀚海行产品图标由带黑色画布的 RGB PNG 改为真实透明通道抠图，保留学生、书舟、校徽船帆与海浪主体；首页、侧栏和虚拟形象预览同步移除白色细边、底色和内边距。
- Hub 演示首页新增模型路由状态条，直接展示当前 Profile、掩码 API Key、全局默认模型、瀚海行专属绑定和可用模型数量，并可一键进入完整配置中心；明文密钥仍只在服务端加密保存。
- 瀚海行独立运行时在模型目录冷启动后自动执行安全模型发现，成功时可直接切换 `gpt-5.6-luna`、`gpt-5.6-sol`、`gpt-5.6-terra`，上游不可用时继续回退默认模型，不阻塞页面；Luna 与 Terra 真实调用均返回对应模型且未降级。
- 新增透明 PNG、Hub 首页模型状态、本地自动发现与失败回退测试；自动化回归共 218 项通过，三服务固定验收 10/10 轮通过，真实浏览器覆盖桌面、390 px 移动端和 Luna/Terra 切换且无控制台错误。

### `feat`：瀚海行首页模型选择与平台真实切换

- 首页产品图标从 72 px 放大到 88 px，标语改为“真理如瀚海 求索亦行舟”，两个五字词组保持清晰间距并在移动端按词组边界换行。
- Hub Featured 工作台下发当前绑定 Profile 内的安全模型目录；瀚海行首页改用原生下拉选择，至少可展示 `gpt-5.6-luna`、`gpt-5.6-sol`、`gpt-5.6-terra`。
- Grant 兑换新增可选 `requested_model_id`，Hub 只允许选择当前 Profile 内仍具备聊天资格的模型；平台调用、流式返回和最终模型标识会跟随本轮选择，旧 Connected 请求保持兼容。
- 模型目录不返回 API Key、密文、Profile 标识、Base URL、指纹、元数据或委托 token；模型与思考强度控件统一为柔和胶囊样式。
- 新增模型切换、越权模型拒绝、平台目录隔离和本地回退回归测试；设计文档同步记录兼容边界和验收负例。
- 修复 Responses 流偶尔只在 `response.completed` 携带完整正文与 usage 时被误判为空回答的问题；Hub 现在会从完成事件安全补齐未发送的正文和嵌套用量。

### `feat`：五项核心优化最终打磨与交付验收

- 保持自托管字体、默认深色主题、瀚海行工作台直达和管理员/开发者/普通用户三层权限不变，并用真实浏览器重新核对三种身份的导航与后端边界。
- 多模型配置中心新增“Profile → 模型发现 → 全局默认 → Agent 专属绑定”四步进度概览，展示可聊天模型、全局绑定和兼容 Agent 状态；API Key 继续只在服务端加密保存，浏览器仅显示遮罩与指纹。
- 瀚海行 Manifest 升级为 `0.9.0`，正式声明 `platform-model-gateway` 与 `platform_optional` 运行时，修复已有 Hub 数据库因旧 `0.8.0` Manifest 无法建立 Agent 专属模型绑定的问题。
- 瀚海行知识广场升级为六门课程的响应式课程集市：加入本地课程封面、资料数、更新时间和“真实可检索 / 演示入口”状态；数学分析 B1 继续使用 25 份真实资料，其余课程不伪造内容。
- 修复课程封面静态路径 404、透明插画裁切、幂等 seed 伪刷新更新时间、包版本与健康接口版本不一致，以及浏览器缓存旧页面资源的问题。
- 固定验收脚本增加模型能力声明、Profile/绑定可用性和密钥泄漏检查，并兼容 Hub 返回的列表式 Agent 绑定；Grant 签发时会重新确认旧绑定模型仍具备聊天资格。本地真实 `gpt-5.6-sol` 平台模型链路与 Featured 工作台通过。
- 自动回归共 216 项通过；三服务固定验收 10/10 轮通过。真实浏览器覆盖 Hub 首页、Agent 广场、三种身份、模型设置、瀚海行直达、六门课程知识广场和 390 px 移动端，无控制台错误或横向溢出。

### `feat(course-agent)`：瀚海行启用学生求索主题新图标

- 正式图标替换为“科大学生站在书页之舟上驶向瀚海”的具象方案，强化学生、课程知识、求索行舟和中科大身份四层含义。
- 船帆使用与帆面透视和水粉纹理融合的中科大校徽，保留完整环形结构、书帆图案和 `1958`；水面扩展为三层宽阔蓝色波带，使“瀚海”意象更加充分。
- 保持现有 `256×256` RGB PNG 资产契约，并提升首页、侧栏、浏览器图标和虚拟形象预览的缓存版本，避免客户端继续显示旧图。

### `fix(course-agent)`：统一瀚海行品牌文案

- 首页标语统一为“真理如瀚海求索亦行舟”，移除标题中间的逗号。
- 产品界面、虚拟形象预览、OpenAPI 标题、测试与协作文档中的品牌名统一为“瀚海行Agent”，不再混用小写 `agent`。

### `feat(course-agent)`：瀚海行整站视觉对齐 Campus Agent Hub

- 在五项核心优化版本上统一瀚海行的深空黑背景、蓝紫强调色、字体层级、留白、边框、卡片、按钮、输入框、导航和弹窗，使问答首页、知识库、知识广场、日程表、个人设置与文档阅读器形成同一套视觉语言。
- 保留瀚海行品牌、课程知识工作台定位和小比熊虚拟形象；首页仅在核心标题使用 Noto Serif SC，其余高密度工具界面继续使用 Inter 与 Noto Sans SC，避免机械复制 Hub。
- 不改动页面 DOM、业务接口和状态链路；资料选择、RAG、模型切换、PDF 缩放、文本字号、日程弹窗、身份权限、虚拟形象拖动与滚轮缩放等原有能力保持可用。
- 修复 390 px 首页模型来源徽标与模型输入框重叠、知识库标题断行等响应式细节；深色与浅色主题均补齐 `1440x900`、`390x844` 主要页面截图，所有检查页面无横向溢出，浏览器控制台无错误或警告。
- 瀚海行 Python 108 项、JavaScript 12 项，以及 Hub、Demo Agent 和 Contract 测试合计 207 项全部通过；重新按完整 Hub 工作台参数启动后，固定三服务 Demo 验收连续 10/10 轮通过。

## 2026-08-18

### `12b964f` `feat`：交付五项核心功能优化

- Hub 与瀚海行统一使用仓库内自托管的 Inter、Noto Sans SC 和 Noto Serif SC 字体，首次访问继续默认进入已正式设计的深色界面，不依赖远程字体服务。
- Agent 广场中的瀚海行“立即对话”改为直接完成 Featured 授权并进入正式工作台，不再经过中间聊天页。
- Hub 明确区分管理员、开发者和普通用户：管理员拥有审核模块，开发者拥有提交与“我的提交”，普通用户只保留基础使用与模型配置；后端接口继续执行独立权限校验。
- 新增服务端多 Model Profile、模型发现、全局与 Agent 专属绑定、加密密钥存储和 Hub Model Gateway；瀚海行以 `platform_optional` 方式使用短期授权，保留独立配置回退，第三方旧 Agent 无需改造。
- 瀚海行知识广场新增六门课程入口：数学分析 B1 关联 25 份真实可检索资料，线性代数、概率论与数理统计、大学物理、数据结构和程序设计基础明确标记为“演示知识库 / 资料待补充”，不伪造资料数量。
- 修复模型网关旧 SQLite 迁移、委托撤销请求、Profile 轮换失效、Connected run 生命周期、`featured_workspace` 与 `expired` 持久状态等交付审计问题。
- 最终自动化回归共 207 项全部通过；三服务健康正常，固定 10 轮 Demo 验收两次分别为 10/10 和 9/10，均达到至少 9 轮成功的门槛。
- 真实浏览器验证两套 Profile 分别调用 `gpt-5.6-sol` 与 `gpt-5.6-terra`；单选《第1-3章复习提纲》的 RAG 回答返回页码引用。管理员、开发者、普通用户、课程集市、空资料状态及桌面/移动端布局均完成截图审计。

### `aa1c6a3` `docs`：五项核心功能优化设计与验收方案

- 新增五项核心优化设计文档，明确深色字体体系、Agent 广场直接进入、三层身份权限、多模型配置兼容与瀚海行课程知识广场的 To-do list。
- 将本轮优化限定为在现有 Hub、瀚海行和三服务 Demo 基线上做增量提升，要求不得破坏既有可运行链路。
- 为每个 To-do 固定代码检查、自动测试、真实部署、浏览器验收和截图审计门禁，并规划统一审计证据目录。
- 明确实施顺序：先冻结基线，再处理直接进入、字体、权限、课程广场，最后单独实施风险最高的多模型配置。

### `a5b5f5f` `feat(course-agent)`：本地三服务启动与模型接口兼容

- 新增 Linux/macOS 非 Docker 启动脚本，按 Hub 注册、运行时凭据生成、两个 Agent 启动和完整协议验收的顺序建立三服务闭环。
- 瀚海行模型适配器新增 `responses` 与 `chat_completions` 两种 API style，支持 Responses API 和通义千问等 OpenAI Chat Completions 兼容服务。
- Chat Completions 路径补齐非流式与 SSE 流式响应解析，并兼容 `prompt_tokens`、`completion_tokens` 用量字段。
- bootstrap 新增仅注册模式，使 Featured Agent 可以在应用启动前获得运行时密钥；原有密钥文件热加载能力保持有效。

### `5072610` `fix(course-agent)`：补齐 API style 交付链路

- 修复 API style 只存在于模型适配器、却没有传入 Docker、本地启动和设置接口的问题；管理员现在可在瀚海行设置页选择 Responses 或 Chat Completions 并持久化。
- 模型自动发现允许选择服务端返回的普通文本模型，同时继续禁用图像、音频、实时、向量、重排、语音转写和审核等非聊天模型。
- 修复本地脚本重复启动时无法正确停止 Demo Agent，并在缺少 `.env` 时自动从示例配置创建本地文件。
- 自动化验证为瀚海行 Python 97 项、Hub Python 26 项、第三方 Demo Python 7 项、JavaScript 31 项、Contract 5 项，共 166 项全部通过；三服务固定验收 10/10 轮成功，真实浏览器验证 Hub 进入瀚海行、设置控件、GPT-5.6 回答和上下文用量均正常。

## 2026-08-17

### `d877092` `fix(course-agent)`：Hub 工作台密钥轮换热加载

- 瀚海行在每次 Featured 工作台授权码兑换前重新读取运行时 secret 文件，不再把 bootstrap 前的旧密钥永久缓存在进程内。
- 显式 `COURSE_AGENT_HUB_CLIENT_SECRET` 继续保持原有静态优先级；secret 文件缺失或为空时明确拒绝兑换，不回退旧文件密钥。
- 新增密钥文件轮换回归测试；完整自动化 164 项全部通过。
- 真实部署将 Hub 旧密钥宽限期设为 0，bootstrap 旋转 secret 后保持瀚海行 PID 不变，三种身份连续 10/10 轮完成聊天、工作台进入和授权码重放拒绝。
- `2134fe5` 已将本次修复、测试结论和后续维护经验同步写入根级与课程 Agent 更新日志。

## 2026-08-09

### `c75c160` `fix(course-agent)`：OCR 资料保存原子性与发布前审计

- 修复“保存至个人库”与审计事件分两次提交的问题；文档、页面、分块、FTS 和审计事件现在在同一事务内提交，任一环节失败都会整体回滚并清理复制文件。
- 复制 OCR 解析过的 PDF 时同步保留经过校验的 `.ocr.md` sidecar；个人资料副本和知识库发布快照不再退化为 PyMuPDF 乱码文本，删除副本时也会一并清理 sidecar。
- 补齐资料单项勾选的三界面同步，知识库资料行、知识库选择器与首页知识检索选择器保持同一状态。
- 审计确认“保存至个人库”继续要求下载权限，不会把仅可预览的订阅资料复制到个人空间。
- 自动化验证通过：瀚海行 Python 94 项、Hub Python 26 项、第三方 Demo Python 7 项、两组 JavaScript 31 项；三服务固定验收 10/10 轮成功，真实浏览器验证 Agent 广场、GPT 回答、资料勾选同步和文档缩放均正常。

### `d464a85` `fix`：队友最新提交拉取后的交付修复

- 拉取并保留队友的模型配置精简、资料新标签页查看、资料勾选入口、页面标题和视觉调整。
- 补齐“保存至个人库”后端闭环：只有具备下载权限的非个人资料可显示保存入口；保存后重新建立页面、分块与 FTS 索引，重复保存会给出明确提示并记录审计事件。
- 修复 Hub 清空模型配置后表单仍显示旧值，以及资料列表、首页资料选择器和知识库选择器勾选状态不同步的问题。
- 恢复“不展示隐藏推理”边界；模型推理事件和 `reasoning_content` 不进入前端或历史记录，只展示最终回答。
- 模型默认最大输出保持 `1200` token，可用 `COURSE_AGENT_LLM_MAX_OUTPUT_TOKENS` 调整；非法值回退默认值，异常大值限制到 `32000`。
- 自动化验证通过：瀚海行 Python 92 项、Hub Python 26 项、第三方 Demo Python 7 项、两组 JavaScript 31 项。

## 2026-08-08

### `20e4485` `fix(hub)`：模型配置页精简，并补交 course-agent 前端改动

- Hub「模型配置」页只保留默认模型厂商、模型名称、API Base URL、API Key 四项输入；移除调用温度、最大输出 token 字段，以及右侧「说明 / 生效范围 / 安全」整块面板与“测试连通”按钮。
- 同步精简前端设置读写逻辑（提交数据、本地读取与清空仅保留上述四项），保留“清空”与“保存配置”两个操作按钮。
- 补交此前未提交的 course-agent 改动：推理事件支持可折叠推理（reasoning）、文件“重解析”改为“保存至个人库”、页面标题更新为“瀚海行Agent”、源列表选择器随操作同步刷新。

### `25d8524` `feat(ui)`：Hub 与瀚海行默认深色主题

- 两套产品在没有有效主题偏好时统一使用正式设计的深色界面；非法或损坏的主题记录也会安全回退深色。
- 保留用户主动选择浅色的结果，Hub 继续支持“跟随系统”；主题在 CSS 加载前恢复，避免首屏显示错误主题。
- Hub 星空只响应合法的深色、浅色或跟随系统状态，非法值不会再被误认为跟随系统。
- 新增可执行主题测试，覆盖首次访问、显式选择、非法记录、存储异常和首屏加载顺序。

### `451d693` `feat(hub)`：门户、个人主页与模型配置更新

- Hub 拆分为平台主页、Agent 广场、模型配置和个人主页，补充更清晰的页面导航与入口。
- 新增头像裁剪、昵称、签名、个人简介和常用 Agent 钉选，Hub 顶栏会同步显示个人资料。
- 开屏动画改为同一浏览器会话只播放一次；品牌标语更新为“真理如瀚海求索亦行舟”。
- 瀚海行模型控件改为可自由输入并提供候选项，不再自动选中默认模型。

### `87f6aa8` `fix(hub)`：新门户部署验收修复

- 修复个人主页把 Agent 数组误当作分组对象、按钮在渲染前绑定事件的问题；常用 Agent 现在可反复钉选、取消并立即保存。
- 同步瀚海行自动化测试与冒烟检查中的新版标语、静态资源版本和模型输入控件断言。

## 2026-08-06

### `ac99408` `fix(hub)`：统一聊天故障收口

- 修复 Hub 密钥轮换后瀚海行和第三方 Demo Agent 缓存旧 JWKS、持续返回 401 的问题；保持完整 JWT 鉴权，只在签名失败时刷新并重试一次。
- 统一聊天限制为单个活动请求；发送期间禁用重复提交，取消和失败都会正确收尾等待气泡。
- `RUN_ERROR`、异常 SSE 结尾和重试流程统一处理；重试绑定原 Agent 与原问题，并清理旧错误卡片。
- 同步更新前端 ES Module 版本，修复浏览器缓存旧 `hub-core.js` 导致广场主区域空白的问题。
- 瀚海行 87 项、Hub 26 项、第三方 Demo 7 项、Hub JavaScript 9 项测试通过；三服务固定验收连续 10/10 轮成功。

### `435f735` `feat(hub)`：深夜智能舱门户改版

- 重构 Agent 广场首屏、导航、卡片、聊天、开发者接入和管理审核页面的视觉层级。
- 新增固定种子 Canvas 星空、随机星团、迷雾拨开和鼠标附近星辉效果。
- 增加减少动态效果、浅色主题、移动端布局和资源销毁降级，并完成桌面与手机浏览器验收。

### `3302fd2` `feat`：Hub 治理与一致性验收

- Manifest 新版本提交后自动运行 URL、健康、聊天和协议检查，未通过时不能批准上线。
- Gateway 增加大小限制、SQLite 持久限流、健康失败准入、严格 AG-UI 生命周期、取消和用量审计。
- 增加 Featured 凭据轮换与撤销、安全图标缓存、固定比赛验收脚本和授权码防重放验证。

### `6538eb8` `feat`：Campus Agent Hub 三服务闭环

- 新增独立 Hub 服务、Contract v1、Registry、审核状态机、Gateway、Agent Portal 和统一聊天容器。
- 瀚海行以原生 AG-UI 接入；独立校园助手以 `simple-chat` 接入，证明新增 Agent 不需要修改 Gateway 业务路由。
- 新增 Featured 工作台授权、EdDSA JWT/JWKS、Docker Compose 编排、自动注册审核和一键演示入口。

### `076ce98`、`9074745` `docs`：Hub 视觉改版规范

- 冻结深夜智能舱视觉方向、交互边界、响应式规则、无障碍降级和验收标准。
- 审计并补充星空密度、动画生命周期、主题切换和移动端布局契约。

## 2026-08-05

### `fbbc55f` `docs`：Agent Hub 顶层架构

- 明确当前产品采用用户主动选择 Agent、平台确定性转发的插件化模式，不实现 Main Agent 自动路由或 Agent 递归协作。
- 定义 Link App、Connected Agent、Featured Agent 三种接入等级及比赛 Demo 演示流程。

### `d2c86a8` `feat(course-agent)`：PetPresence 小比熊虚拟形象

- 引入团队自有 PetPresence 项目的小比熊形象、多个动作和播放失败海报。
- 接入待机、思考、点击和陪伴学习状态，并支持悬停滚轮缩放、拖动和持久化。

### `65e0cf0` `fix(course-agent)`：虚拟形象悬浮布局

- 将虚拟形象改为右下角独立悬浮，避免长期占用聊天区宽度或遮挡输入控件。
- 保留拖动、缩放、键盘操作和响应式边界，并撤销同批次中无关的模型与权限变化。

### `cd8a392` `feat`：Hub 框架资料与启动视觉

- 加入 `HUB_FRAMEWORK_SPEC.md`、Hub 启动页和中科大校徽资源，为后续平台闭环实现建立基础。

## 2026-08-04

### `5b72e14` `feat(course-agent)`：真实流式模型回答

- 主问答和独立解释分支改为 POST + SSE 真流式输出，保留旧 JSON 端点兼容性。
- 支持取消、身份切换隔离、异常 EOF、上游失败和部分回答收尾，不展示隐藏推理。

## 更早记录

- [课程资料 Agent 完整更新日志](./apps/course-agent/CHANGELOG.md)
- [2026-07-24 至 2026-07-30 周度提交总结](./docs/weekly-change-summary-2026-07-24-to-2026-07-30.md)
- `git log --oneline --decorate`：查看仓库全部提交及分支标签。
