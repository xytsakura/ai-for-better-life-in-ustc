# AI for better life In USTC 作品简介

> **项目源码仓库（GitHub，公开）：** <https://github.com/xytsakura/ai-for-better-life-in-ustc>

## 一、作品背景

校园中的课程复习、办事指引和生活服务分散在不同平台，学生往往需要反复寻找合适的工具；不同 Agent 的接口、认证方式和交互协议也不统一，难以形成可持续扩展的校园智能服务体系。

同时，课程资料包含个人笔记、小组资料、扫描版 PDF 和历年试题。直接将文件交给大模型，容易出现权限混乱、文本识别不完整、回答缺少来源以及删除资料后仍被旧索引命中等问题。为此，本作品采用“校园 Agent 平台 + 课程复习 Agent”的双层方案：AI for USTCERS 负责统一接入和管理校园 Agent，“瀚海行”负责完成课程资料管理与可信复习问答。

## 二、解决的问题

- **服务入口分散**：通过 Agent 应用广场集中展示已审核、已上线的校园 Agent。
- **工具选择困难**：提供普通对话和需求分析路由，根据用户需求推荐合适的 Agent，并由用户确认后进入。
- **Agent 接入成本高**：使用统一 Contract、Gateway 和交互协议，使不同技术栈的 Agent 可以接入同一平台。
- **课程资料难整理、难检索**：建立个人、共享和订阅三类知识空间，对 PDF 进行解析、OCR、分块和全文检索。
- **模型回答难以验证**：要求用户选择本轮使用的资料，并在回答中展示文件名、页码和引用编号。
- **隐私和权限边界不清**：在检索和模型调用前完成权限校验，资料删除、取消订阅或版本切换后立即停止命中旧内容。

## 三、核心功能

1. **AI for USTCERS**：支持 Agent 注册、契约验收、管理员审核、健康检查、应用广场、暂停恢复和版本回滚。
2. **智能需求路由**：普通问题由模型直接回答；专业需求从已审核且在线的 Agent 中生成受控推荐，模型不能自行生成跳转地址或调用未审核服务。
3. **“瀚海行”课程知识库**：提供个人知识库、邀请制共享知识库和只读订阅知识库，当前部署版本支持 PDF 上传、解析、去重和删除。
4. **OCR 与课程检索**：使用 DeepSeek-OCR-2 处理扫描版 PDF，通过 SQLite FTS5 和 jieba 完成中文全文检索。数学分析 B1 演示库包含 25 份 PDF、510 页资料和 686 个检索分块。
5. **选择性 RAG 与来源引用**：用户逐份选择本轮允许使用的资料，系统只在通过权限校验的文件中检索，并返回可定位到原 PDF 页面的引用回答。
6. **知识广场**：支持资料投稿、管理员审核、订阅、版本更新、暂停、下架和回滚，公开订阅权限与学习小组权限相互独立。
7. **统一交互与部署**：原生 Agent 使用 AG-UI，轻量 Agent 使用 `simple-chat` 并由 Gateway 统一适配；项目支持三服务一键启动和固定流程验收。当前演示已部署到阿里云 ECS，并提供公网入口：平台 `http://39.106.98.62:4000`、瀚海行 Agent `http://39.106.98.62:4001`、Demo Agent `http://39.106.98.62:4002`。

## 四、使用的模型及 API 调用方式

- **`gpt-5.6-sol`**：当前演示环境默认用于普通对话、需求路由、直接问答、资料问答和独立解释；由后端调用 Responses-compatible API。
- **`deepseek-ai/DeepSeek-OCR-2`**：将扫描版 PDF 页面识别为 Markdown 文本；OCR 脚本调用内网 `/v1/ocr` HTTP 接口。
- **SQLite FTS5 + jieba**：中文课程资料检索与候选片段召回；在“瀚海行”后端本地执行，不属于生成模型调用。

浏览器不直接访问模型服务。生成模型的调用链按运行环境分为两条：

```text
Hub 首页：浏览器 -> Hub -> Platform Model Gateway -> 模型 API
瀚海行：浏览器 -> 瀚海行后端 -> Platform Model Gateway -> 模型 API
                         \-> 瀚海行服务端回退配置 -> 模型 API
```

平台网关可用时，Hub 和瀚海行优先通过网关获得短时授权后调用模型；网关未配置或暂时不可用时，瀚海行可以使用服务端的回退模型配置完成调用。服务端向规范化后的基础地址发送 `/responses` 请求，通过 `Authorization: Bearer <API Key>` 鉴权，并使用 `stream=true` 获取 SSE 增量结果；如果基础地址是根地址，运行时会先补齐 `/v1`，已经带 `/v1` 的地址则直接追加 `/responses`。系统只向前端转发必要的流式事件、回答正文和受控推荐结果，API Key 不进入浏览器、Git 仓库或普通日志。对于只支持 Chat Completions 的兼容服务，也可以切换到 `/chat/completions` 接口。

OCR 处理时，脚本先将 PDF 页面渲染为 PNG，再以 `multipart/form-data` 方式调用 `/v1/ocr`，最终生成带源文件 SHA-256、页码和模型信息的 `.ocr.md` 文件，用于检索；引用和预览仍指向原 PDF。

## 五、创新点阐述

1. **平台与标杆 Agent 结合**：既完成可扩展的校园 Agent 平台，又用“瀚海行”验证真实课程复习业务，不停留在应用卡片或简单聊天演示。
2. **技术栈无关的插件化接入**：复杂 Agent 可以使用 AG-UI，轻量 Agent 可以使用 `simple-chat`；新增 Agent 只需实现契约并注册，无需修改 Gateway 业务路由代码。
3. **自然语言理解与确定性治理分离**：模型只负责理解需求和提出候选，Registry 决定 Agent 是否可用，服务端二次校验，用户完成最终确认，减少模型幻觉和黑盒调用风险。
4. **用户可控的选择性 RAG**：模型只能看到用户有权访问且本轮明确选择的资料，回答能够回到具体文件和页码，兼顾隐私、准确性与可追溯性。
5. **OCR 文本与原始证据分离**：OCR Markdown 用于提升检索质量，原 PDF 用于引用和预览；源文件变化时，过期 OCR 会因哈希校验失败而自动失效。
6. **安全和权限内建**：模型密钥仅由服务端管理，资料在模型调用前完成权限过滤，Agent 还具有审核、健康检查、限流、暂停和回滚机制。
