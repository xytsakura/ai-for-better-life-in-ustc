# AI for better life In USTC 作品简介

## 作品名称

AI for better life In USTC：Campus Agent Hub 与瀚海行课程复习 Agent

## 作品简介

校园中的课程复习、课程评价、办事指引和生活服务往往分散在不同入口，学生需要反复寻找适合自己的工具，不同智能体之间也缺少统一的接入方式。`AI for better life In USTC` 面向中国科学技术大学学生，构建了一个可注册、可审核、可展示、可路由和可统一调用的校园 Agent 集成平台。

平台提供 Agent 应用广场、版本化接入契约、注册审核、健康检查、统一 Gateway、模型配置和首页需求分析路由。用户可以直接进行普通对话，也可以输入“我要复习数学分析这门课”“我需要知道在哪里签字盖章”等需求，由平台从已审核上线的 Agent 中推荐合适的服务，并通过受控入口直接进入目标 Agent。

瀚海行是平台的第一方标杆 Agent，围绕数学分析 B1 课程提供个人、共享和订阅知识库。系统支持 PDF、PPTX 基础文本、Markdown、TXT 和 OCR 资料处理，允许用户勾选本轮需要使用的课程资料，通过 SQLite FTS5 检索生成带文件名和页码的引用回答，并支持 PDF 原页、提取文本和页面缩放。当前数学分析 B1 演示库包含 25 份课程资料。

平台通过服务端 Model Gateway 调用 Responses-compatible 大语言模型 API，当前演示默认使用 `gpt-5.6-sol`，并支持发现和切换多个兼容模型，包括 `gpt-5.6-luna` 与 `gpt-5.6-terra`。模型请求由后端统一管理，API Key 不暴露给浏览器；Hub、瀚海行和轻量 Demo Agent 通过 SSE / AG-UI 向前端增量返回回答。

本作品的主要创新包括：第一，从单一复习工具扩展为面向校园场景的 Agent 应用广场；第二，用统一 Contract 和 `simple-chat` / AG-UI 适配器降低不同 Agent 的接入成本；第三，将自然语言需求发现与平台审核、在线状态和用户确认结合，避免黑盒式自动调用；第四，将 OCR、选择性 RAG、知识空间权限、来源引用和删除失效组织成可验证的课程知识库闭环。瀚海行是当前最完整的业务实现，评课社区和校园公共服务 Agent 作为可接入的 Demo，为后续真实数据源和更多校园服务扩展预留接口。

## 使用模型与 API 调用方式

- 默认演示模型：`gpt-5.6-sol`；
- 可选模型：包括 `gpt-5.6-luna`、`gpt-5.6-terra` 等平台发现的兼容模型；
- API 形态：Responses-compatible 模型 API；
- 调用路径：浏览器 -> Hub / 瀚海行后端 -> Platform Model Gateway -> 模型 API；
- 输出方式：SSE / AG-UI 流式事件；
- 密钥管理：API Key 仅在服务端运行时配置和使用，不提交到仓库，也不下发给浏览器。

## 当前演示范围

当前演示包含 Campus Agent Hub、瀚海行、校园助手 Demo、评课社区 Demo 和校园公共服务 Demo。评课社区和校园公共服务目前是明确标注的 Future Work Demo，不宣称已经接入真实点评或行政数据库。
